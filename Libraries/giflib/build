#!/usr/bin/env modbuild

pbuild::configure() {
	"${SRC_DIR}"/configure \
		--prefix="${PREFIX}" \
		|| exit 1
}

pbuild::add_to_group 'Libraries'
pbuild::set_build_dependencies "${COMPILER}"
pbuild::make_all
